apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cert-manager.io/inject-ca-from: azureserviceoperator-system/azureserviceoperator-serving-cert
    controller-gen.kubebuilder.io/version: v0.18.0
  labels:
    app.kubernetes.io/name: azure-service-operator
    app.kubernetes.io/version: v2.20.0-hcpclusters.1
  name: applications.entra.azure.com
spec:
  conversion:
    strategy: Webhook
    webhook:
      clientConfig:
        service:
          name: azureserviceoperator-webhook-service
          namespace: azureserviceoperator-system
          path: /convert
          port: 443
      conversionReviewVersions:
      - v1
  group: entra.azure.com
  names:
    categories:
    - azure
    - entra
    kind: Application
    listKind: ApplicationList
    plural: applications
    singular: application
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Application is an Entra Application.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              description:
                description: 'Description: The description of the application.'
                type: string
              displayName:
                description: 'DisplayName: The display name of the application.'
                type: string
              groupMembershipClaims:
                description: 'GroupMembershipClaims: Configures the groups claim issued
                  in a user or OAuth 2.0 access token.'
                type: string
              identifierUris:
                description: 'IdentifierUris: The URIs that identify the application
                  within its Azure AD tenant, or within a verified custom domain.'
                items:
                  type: string
                type: array
              isFallbackPublicClient:
                description: 'IsFallbackPublicClient: Specifies the fallback application
                  type as public client.'
                type: boolean
              operatorSpec:
                description: 'OperatorSpec: The operator specific configuration for
                  the resource.'
                properties:
                  configmaps:
                    description: ConfigMaps specifies any config maps that should
                      be created by the operator.
                    properties:
                      appId:
                        description: 'AppId: The application (client) ID of the application.'
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations is an optional set of annotations
                              to apply to the ConfigMap.
                            type: object
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          labels:
                            additionalProperties:
                              type: string
                            description: Labels is an optional set of labels to apply
                              to the ConfigMap.
                            type: object
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      entraID:
                        description: 'EntraID: The Entra ID (object ID) of the application.'
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations is an optional set of annotations
                              to apply to the ConfigMap.
                            type: object
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          labels:
                            additionalProperties:
                              type: string
                            description: Labels is an optional set of labels to apply
                              to the ConfigMap.
                            type: object
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  creationMode:
                    description: |-
                      CreationMode: Specifies how ASO will try to create the resource.
                      Specify "AlwaysCreate" to always create a new application when first reconciled.
                      Or specify "AdoptOrCreate" to first try to adopt an existing application with the same display name.
                      If multiple applications with the same display name are found, the resource ready condition will show an error.
                      If not specified, defaults to "AdoptOrCreate".
                    enum:
                    - AdoptOrCreate
                    - AlwaysCreate
                    type: string
                type: object
              publicClient:
                description: 'PublicClient: Public client (desktop/mobile) platform
                  configuration.'
                properties:
                  redirectUris:
                    description: 'RedirectUris: Redirect URIs for public client applications.'
                    items:
                      type: string
                    type: array
                type: object
              signInAudience:
                description: 'SignInAudience: Specifies the Microsoft accounts that
                  are supported for the application.'
                enum:
                - AzureADMyOrg
                - AzureADMultipleOrgs
                - AzureADandPersonalMicrosoftAccount
                - PersonalMicrosoftAccount
                type: string
              spa:
                description: 'Spa: Single-page application platform configuration.'
                properties:
                  redirectUris:
                    description: 'RedirectUris: Redirect URIs for single-page applications.'
                    items:
                      type: string
                    type: array
                type: object
              tags:
                description: 'Tags: Custom strings for categorizing and identifying
                  the application.'
                items:
                  type: string
                type: array
              web:
                description: 'Web: Web platform configuration for the application.'
                properties:
                  implicitGrantSettings:
                    description: 'ImplicitGrantSettings: Settings for implicit grant
                      flow.'
                    properties:
                      enableAccessTokenIssuance:
                        description: 'EnableAccessTokenIssuance: Whether to enable
                          access token issuance in the implicit flow.'
                        type: boolean
                      enableIdTokenIssuance:
                        description: 'EnableIdTokenIssuance: Whether to enable ID
                          token issuance in the implicit flow.'
                        type: boolean
                    type: object
                  redirectUris:
                    description: 'RedirectUris: Redirect URIs for web applications.'
                    items:
                      type: string
                    type: array
                type: object
            required:
            - displayName
            type: object
          status:
            properties:
              appId:
                description: 'AppId: The application ID assigned by Entra.'
                type: string
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              description:
                description: 'Description: The description of the application.'
                type: string
              displayName:
                description: 'DisplayName: The display name of the application.'
                type: string
              entraID:
                description: 'EntraID: The GUID identifying the resource in Entra'
                type: string
              groupMembershipClaims:
                description: 'GroupMembershipClaims: Configures the groups claim issued
                  in a user or OAuth 2.0 access token.'
                type: string
              identifierUris:
                description: 'IdentifierUris: The URIs that identify the application
                  within its Azure AD tenant, or within a verified custom domain.'
                items:
                  type: string
                type: array
              isFallbackPublicClient:
                description: 'IsFallbackPublicClient: Specifies the fallback application
                  type as public client.'
                type: boolean
              publicClient:
                description: 'PublicClient: Public client (desktop/mobile) platform
                  configuration.'
                properties:
                  redirectUris:
                    description: 'RedirectUris: Redirect URIs for public client applications.'
                    items:
                      type: string
                    type: array
                type: object
              signInAudience:
                description: 'SignInAudience: Specifies the Microsoft accounts that
                  are supported for the application.'
                enum:
                - AzureADMyOrg
                - AzureADMultipleOrgs
                - AzureADandPersonalMicrosoftAccount
                - PersonalMicrosoftAccount
                type: string
              spa:
                description: 'Spa: Single-page application platform configuration.'
                properties:
                  redirectUris:
                    description: 'RedirectUris: Redirect URIs for single-page applications.'
                    items:
                      type: string
                    type: array
                type: object
              tags:
                description: 'Tags: Custom strings for categorizing and identifying
                  the application.'
                items:
                  type: string
                type: array
              web:
                description: 'Web: Web platform configuration for the application.'
                properties:
                  implicitGrantSettings:
                    description: 'ImplicitGrantSettings: Settings for implicit grant
                      flow.'
                    properties:
                      enableAccessTokenIssuance:
                        description: 'EnableAccessTokenIssuance: Whether to enable
                          access token issuance in the implicit flow.'
                        type: boolean
                      enableIdTokenIssuance:
                        description: 'EnableIdTokenIssuance: Whether to enable ID
                          token issuance in the implicit flow.'
                        type: boolean
                    type: object
                  redirectUris:
                    description: 'RedirectUris: Redirect URIs for web applications.'
                    items:
                      type: string
                    type: array
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
